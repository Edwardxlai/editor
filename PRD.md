# 产出 2：方案设计 —— AI 增强内容复盘工具

> 题目要求说明六件事,本文一节答一件,**每节第一段就是答案**,后面才是理由。
> 需求澄清见 `分析/产出1_需求澄清.md`;怎么跑见 `README.md`;完整推演过程见 `分析/PRD_完整版.md`。

**一句话**:把一场访谈的时间序逐字稿,重排成文章序的编辑工作包 —— 判断、证据、结构、初稿各就各位,最后一层取舍留给编辑。

**术语**:全文只有两种编号,不要混。**字幕号** = SRT 每块自带的序号(即题目说的「段落编号」),本次 1–97。**历史笔记段** = 历史素材被切开后的段落,用于复用匹配,与逐字稿无关。

---

## 一、每一步的输入、输出和 AI 介入方式

> **答案**:分两个阶段。**离线**把历史素材建成段表,一篇一次调用,建完永久复用;**在线**跑一次访谈,6 次模型调用产出 7 份文件。AI 介入方式**全部是 Prompt** —— 没有 RAG,没有 Agent,没有工具调用。

### 1.1 离线阶段:历史素材入库

| | |
|---|---|
| **输入** | 历史商业笔记(本次 8 篇)+ 已发布样稿(3 篇) |
| **输出** | **历史笔记段表**(段落 + 一句判断句 + 回指原文的字符区间)+ 篇级元数据 + 风格卡 |
| **AI 介入** | Prompt,**一篇一次调用**。8 篇 = 8 次 |
| **触发** | 手动 `python ingest.py`。历史素材是静态的,抽一次永久复用 |

**段表存判断句而非原文片段**:更短、噪音少,对匹配有利。原文由字符区间回指,不复制。

**元数据不冗余到段上**。两层判断天然属于不同粒度:

- **篇级** → 合规约束(时效 / 可引用范围 / 适用读者),作用在 `note_id`
- **段级** → 内容匹配,作用在 `seg_id`

匹配到某段后拿 `note_id` 查回篇级约束,就能得出"这段内容对得上,但按可引用范围只能当框架不能引结论"这种结论。元数据会变(业务方可能随时收紧某篇的可引用范围),冗余就得全表更新。

**入库门禁**:同一篇内容的两个整理版本不能同时进库。库里躺着两份,任何一次复用判断都会重复命中同一批观点。

> **设计方向**:段表与在线阶段的 `material_map` 是同一件事的两种形态 —— 都是"把一份长素材按主题归组、每组回指原文"。今后每完成一篇文章,它的 `material_map` 可以直接沉淀为段表条目,不必单独再跑一次入库。差别只在定位单位:逐字稿用字幕号,历史成稿用字符区间。**统一的前提是 material_map 的每个组头要带一句判断句**(段表靠判断句匹配,标签匹配不了),这一条尚未实现。

### 1.2 在线阶段:跑一次访谈

```
data/interview_transcript.srt
      │
      ├─ Stage 1  解析(纯代码)──────→ 字幕表 × 97
      │
      ├─ Stage 2  观点抽取(LLM×1)──→ evidence 主表 ★
      │                                │
      │   ┌────────────┬───────────────┼──────────────┐
      │   │            │               │              │
  Stage 3        Stage 4          Stage 5         Stage 6
  历史复用        角度生成         结构与初稿      素材脉络
  (LLM×1)        (LLM×1)          (LLM×2)         (LLM×1)
      │            │               │              │
      └────────────┴───────────────┴──────────────┘
                       │
                 Stage 7  渲染(纯代码)
                       ↓
   outputs/  evidence.md  editorial_checks.md  angles.md
             outline.md   draft_v0_5.md  reuse_suggestions.md
             material_map.md
```

| Stage | 输入 | 输出 | AI 介入 |
|---|---|---|---|
| 1 解析 | SRT | 字幕表(号 / 起止时间 / 说话人 / 正文)× 97 | **无**。SRT 是结构化格式,正则即可 |
| 2 观点抽取 ★ | 字幕表**全文** + brief | evidence 主表 | **Prompt**,单次调用,全文一次进 |
| 3 历史复用 | **evidence 主表** + 历史笔记段表 + 篇级元数据 | `reuse_suggestions.md` | **Prompt** |
| 4 角度生成 | **evidence 主表** + brief + 标题句式 | `angles.md`(3 个) | **Prompt** |
| 5a 结构 | **evidence 主表** + 选定角度 | `outline.md` | **Prompt** |
| 5b 初稿 | 选定角度 + outline + evidence 主表 | `draft_v0_5.md` | **Prompt** |
| 6 素材脉络 | 字幕表**全文** + outline 章节 | `material_map.md` | **Prompt**,只输出标签 + 字幕号 |
| 7 渲染 | 各阶段 JSON | 7 个 Markdown | **无** |

**`editorial_checks.md` 不占一次调用。** 它是从 evidence 主表里筛出来的视图 —— 筛"事实类且无出处"的行,加上解析阶段捞到的转录风险项。纯代码渲染,evidence 一出来它就有了。

### ★ 主表原则:读原文只有两次

只有 Stage 2 和 Stage 6 读逐字稿原文,而且读的目的不同:

| | Stage 2 抽取 | Stage 6 素材脉络 |
|---|---|---|
| 做的事 | **观点级压缩** —— 97 条里挑出十几个判断 | **句子级归类** —— 全部内容按主题归组,不做主张 |
| 产物 | 判断 + 证据 | 血肉:案例细节、数字、往复展开、比喻 |
| 编辑什么时候用 | 动笔**前**查:这条站不站得住 | 动笔**中**翻:这一节还能写什么 |

其余四次调用**都不读原文**,拿的是 evidence 主表。这是全流程最重要的一条约束,理由见 1.3。

七个文件与主表的关系:

| 文件 | 关系 |
|---|---|
| `evidence.md` | 主表本身 |
| `editorial_checks.md` | 主表的视图(纯代码筛选) |
| `outline.md` / `angles.md` | 从主表选落点、组结构 |
| `draft_v0_5.md` | 正文内联证据标记,时间戳与说话人由代码补全 |
| `reuse_suggestions.md` | 历史笔记段表 × 主表的匹配结果 |
| `material_map.md` | 按 outline 章节重排的原文,覆盖主表用到和没用到的全部内容 |

### 1.3 三条硬约束

**① 逐字稿不分段。** 1.2 万字任何模型一次读得下,分段会切断跨段关系。实例:字幕 36 的限定语("这里我说的不是某个确定行业马上会发生什么")限定的是字幕 38 的数据 —— 划段极可能把两者分开,第二次调用再也看不见它们的关系。

**② 除抽取与素材脉络外,一律不喂原文。** 把整篇原文丢给模型让它"提炼角度",等于让它自己找重点。它做不到取舍,只能取平均,输出全篇的最大公约数 —— 也就是那句正确的废话("AI 不会取代你,会用 AI 的人会")。**平庸是这个操作本身决定的,不是提示词写得不好。** 主表已经是做完取舍的结果,喂它进去,模型只需在给定落点上选和写。

> 推论:抽取质量直接决定下游全部质量。主表抽漏的东西,后面再也找不回来。**Stage 2 是全流程的质量瓶颈。**

**③ 能用代码算出来的事实,不问模型。** 模型只负责一件不可替代的事:**从原文里挑出哪句话值得引**。

| 项 | 谁提供 | 为什么 |
|---|---|---|
| 引哪句话 | 模型 | 需要判断,代码做不了 |
| 这句话在第几号字幕 | **代码**(按原话反查) | 可算,且模型算错无法被发现 |
| 时间戳、说话人 | **代码**(从字幕表取) | 同上 |
| 这句话是否真的存在于原文 | **代码**(`locate_quote`) | 这是"可溯源"承诺的唯一验证点 |
| 历史笔记是否与本次同源 | **代码**(字符覆盖率) | 可算,且比让模型"感觉像不像"稳定 |

结果是:溯源信息正确**不是靠模型自觉,而是结构上不可能错**;模型唯一还可能犯的错 —— 引了原文里不存在的话 —— 会被当场抓住。**错误面被压缩到一个可测的点上。**

### 1.4 为什么是 6 次,哪些不能合并

成本上每次调用都想省,但有三处合并会直接损坏产物:

| 合并方案 | 判断 | 理由 |
|---|---|---|
| 结构 + 初稿合成一次 | ❌ **不能** | 模型会在写正文时偏离自己刚定的结构,两个文件打架 |
| 角度 + 结构合成一次 | ❌ **不能** | 角度是给编辑**选**的,选完才定结构。合并等于替编辑选了 |
| 抽取 + 素材脉络合成一次 | ❌ **不建议** | 两者的取舍方向相反:一个只留判断,一个全部保留不做主张。同一次调用里模型会互相污染,把血肉写成判断("AI 是把双刃剑") |
| 复用判断不喂原文,只喂主表 | ✅ **已采纳** | 复用匹配的落点本来就必须是主表里的某条要点(见第四节),喂原文既贵又给了模型放水的空间 |

**降本的真正抓手不在合并调用,在不喂原文。** 6 次调用里只有 2 次读全文,其余 4 次读的主表约 3 千字。

### 1.5 为什么不用 RAG / Agent

| 技术 | 不用的理由 |
|---|---|
| **RAG / embedding / 向量库** | 检索是为了"从大量内容里找出相关的少量"。这里两侧都不大:逐字稿 1.27 万字一次读得下,历史笔记段表约 8 千字也一次读得下。**没有需要被检索的规模。** 上向量库只会引入召回损失和额外依赖 |
| **chunking** | 同上,且分块会切断跨段关系 |
| **BM25 / 关键词召回** | 【实测】8 篇笔记的元数据里有 6 篇特征词命中为 **0**。关键词方案在这份数据上直接失效 |
| **Agent 框架** | 流程是固定的 DAG,没有需要模型自己决定下一步的地方 |
| **工具调用** | 无外部工具需求。事实核实交给编辑,不做联网核查 |

**规模边界**:单张段表能撑到百篇量级,8 篇远未触顶。超过之后再分表或上检索层 —— **那是规模问题,不是这一版的问题。** 提前上 RAG 是拿确定的复杂度换不确定的未来收益。

**依赖清单**:一个 HTTP 客户端 + `python-docx`。就这两个。

---

## 二、编辑做什么、AI 做什么

> **答案**:**AI 负责"这话在哪、有没有证据",编辑负责"这话值不值得写、能不能这么说"。**

| 环节 | AI | 编辑 | 为什么这样分 |
|---|---|---|---|
| 通读、结构化、找原话、对时间戳 | ✅ 全做 | — | 机械劳动,AI 快且不会累 |
| 抽取候选要点、打类型标签 | ✅ 提候选 | 审 | AI 提全,编辑砍 —— 漏比多贵 |
| 判断哪条要点值得写 | 提排序建议 | ✅ **定** | 观点价值判断,负责人明确要保留给人 |
| 标出无出处的事实性说法 | ✅ 全做 | 审 | AI 擅长穷举,人擅长判断严重性 |
| **核实**这些说法真假 | ❌ 不做 | ✅ **做** | 无联网、无权威源;AI 核实等于编造置信度 |
| 历史素材匹配、查同源 | ✅ 全做 | 审 | 记忆型劳动,正是编辑现在漏案例的地方 |
| 判断历史素材**能不能用** | 提建议 + 引限制原文 | ✅ **定** | 涉及合规与业务判断 |
| 搭结构、写初稿 | ✅ 出 v0.5 | ✅ **改到 v1** | AI 出骨架,人加理解 |
| 定标题 | 出 3 个备选 | ✅ **选/改** | 标题是立场表达 |
| 发布 | ❌ | ✅ | — |

### 编辑拿到七份文件怎么用

```
动笔前 ── angles 选定方向
       └─ outline 看文章怎么走
       └─ editorial_checks 先过一遍:哪些数字这篇不能写死

动笔中 ── draft 在它上面改
       ├─ 对某条要点存疑 → evidence 查原话、限定语、支撑
       ├─ 想引历史案例   → reuse_suggestions 看哪篇哪段能用、禁止怎么用
       └─ 某一节写不下去 → material_map 翻这一节还有什么原文可用
```

**`editorial_checks.md` 要在动笔前看,不是最后核。** 它列的是"哪些说法全篇无出处",编辑知道了才不会把它写进标题位和结论位 —— 写完再发现要拆结构,返工成本高得多。这也是它不占一次模型调用、随 evidence 一起产出的原因。

**七份文件各做一环,不预演下一环**:outline 里不摆原话(那是 evidence 的活),angles 里不写选题方法论(那是本文的活),checks 里不列时间戳重叠(编辑不会因此改任何一个字)。

---

## 三、如何识别和保留关键观点、案例、因果链与原始证据

> **答案**:LLM 只做一件事 —— **从全文里挑出哪几句话值得引,并说它属于哪一类**。其余全部由代码完成:说话人、时间戳、字幕号由代码回原文反查后**覆盖模型给的值**;类型标签、核实状态由代码按枚举校验;定位不到的引用由代码剔除并记录在案。
>
> 换句话说,**prompt 决定"挑哪句",代码决定"这句到底在哪、是不是真的存在、要不要留"。**

### 一条要点是怎么产生的:七步,标明谁做

以本次实跑的 P01 为例:

| # | 谁做 | 做什么 | 失败会怎样 |
|---|---|---|---|
| 1 | **LLM** | 读 97 条字幕全文,输出 JSON:一句判断句标题、`type`、观点原话、限定语、支撑原话、`verification`、`flags`、`cue_ids` | — |
| 2 | **代码** `repair_quote_ref` | 拿模型给的每一句 `quote` 回逐字稿里搜(规则见下)。搜到 → 用**代码算出的** `cue` / `ts` / `speaker` **覆盖**模型填的值 | 搜不到 → 记为问题,进第 6 步 |
| 3 | **代码** `normalize_cue_ids` | 校验 `cue_ids` 落在 [1,97];模型框漏的(引用在片段之外)自动补进来 | 越界 → 报错 |
| 4 | **代码** `repair_evidence` | 枚举校验:`type` ∈ {观点,案例,因果链,事实};`verification` ∈ {无需核实,需核实,无出处};`point_id` 不重复;`title` 非空 | 任一不合 → 记为问题 |
| 5 | **代码** | `type=事实` 且 `support` 为空 → **自动打 `无出处事实` 标记**。这个标记不是模型打的,是代码按规则推的 | — |
| 6 | **代码** | 有问题就把问题清单连同原 JSON 退回给模型重试(最多 N 次) | — |
| 7 | **代码** `drop_unverifiable` | 重试用尽后:**观点原话**定位不到 → 整条要点剔除;**限定语 / 支撑**定位不到 → 只剔那一条引用,要点保留。**每一条被剔除的都写进 `editorial_checks.md`**,附原话和剔除理由 | — |

**第 7 步是"不伪造"的落点**:不放行任何未经验证的引用,但也不因为一条坏引用丢掉整批好要点;而且**丢了什么必须让编辑看见**,否则就成了静默删除。

### 第 2 步的"搜"是怎么搜的:不是相似度,是归一化后的子串匹配

`pipeline/textutil.py:locate_quote`,三条规则:

1. **归一化**:两边都抹掉全部空白、标点和符号(按 Unicode 类别判断,不是手写字符集)。
   > 实测教训:模型把原文的「"企业家"」写成「'企业家'」,弯引号不在手写集里,一句真实存在的原话被判成杜撰,整批打回。**标点差异不构成"改写原话"**,判据必须完备。
2. **允许省略号**:`quote` 按省略号切成若干片段,**每个片段都必须独立命中,且出现顺序必须一致**(用游标 `find`,不许乱序)。这让模型可以引「他说A……所以B」而不必抄中间三百字。
3. **片段下限 6 个字**:短于 6 字的片段不参与验证,避免「的」「这个」这类噪音假命中。整句都短于 6 字时(如「是啊」)退化为整串匹配。

**没有相似度、没有编辑距离、没有 embedding。** 归一化之后就是精确子串。理由:模糊匹配会让"模型改写了原话"这件事变得可以商量,而这正是唯一一个能抓住模型杜撰的检查点 —— 它必须是二值的。

**所以"识别"这件事里,模型的自由度只有两项**:挑哪句话、打哪个标签。这两项确实没有校验(见第七节),但它们造成的错误面是有限的 —— 挑错了句子,代码会在第 2 步发现它不存在于原文;打错了标签,代码会在第 4 步发现它不在枚举里。**模型能做的最坏的事,是漏掉一条本该抽的要点** —— 这一条目前没有任何机制能发现,是本方案最大的敞口。

### 主表结构

```jsonc
{
  "point_id": "P01",                          // 内部连接键,不面向编辑
  "title": "AI使用者与不使用者的差距正在扩大",   // 判断句,不是名词短语
  "type": "观点",                             // 观点 | 案例 | 因果链 | 事实
  "claim":     { "quote": "…", "speaker": "嘉宾B", "cue": 11, "ts": "00:02:09" },
  "qualifier": { "quote": "…", … },           // 限定语,可为 null
  "support":   [ { "quote": "…", … } ],       // 他为什么这么认为:理由 / 推导 / 举例
  "verification": "无需核实",                  // 无需核实 | 需核实 | 无出处
  "flags": [],                                // 片头 | 口播特征 | 说话人存疑 | 术语不统一
  "why": "给编辑的取舍理由,不参与任何自动校验"
}
```

`type` 用的就是题目自己的词(观点 / 案例 / 因果链 / 事实),不自造分类体系。打标签的唯一目的是**分流到不同输出文件** —— 不在抽取时标,下游每个文件都得重判一次。

### 五条抽取规则

**① 观点原话与支撑原话分开记,支撑一行一条。**
时间戳和说话人是跟着**具体那句话**走的。塞进同一个单元格,初稿就没法在具体句子上挂精确标记。

**② 支撑为空必须显式写"无";但空栏对两类要点意味着不同的事。**

| 类型 | `support` 装的是 | 空着意味着 |
|---|---|---|
| `事实`(含数字、研究、机构、比例) | 他拿什么**证据**支持这个数字 | 🔴 红灯:全篇无出处,写进正文前必须核 |
| `观点` / `案例` / `因果链` | 他**为什么这么认为** | 正常:他直接下了判断没展开,不是缺陷 |

对两者一律打"纯断言"标记是范畴错误 —— 它让「同等教育成本降到约 100 美元」(无出处的事实)和「企业家就是能动性的同义词」(定义式判断,本来就不需要外部背书)长得一模一样。**区分这两件事恰恰是本工具最该做的。**

**③ 限定语挂成字段,不作为独立条目。**
限定语不是判断,是对别的判断的约束,单独抽出来下游无法使用。硬规则:**带限定语的条目,引用主张时必须同时带上限定语。** 丢掉限定语再引用,就会把话写满 —— 正是题目点名的失败模式。

【实测】与同源笔记逐句比对后确认,三句限定语**只存在于 SRT**、不存在于已整理版本:

| 字幕号 | 说话人 | 内容 |
|---|---|---|
| 6 | 主持人 | 未来三年大量白领岗位会被 AI 替代……**但我还没有看到明确证据** |
| 26 | 嘉宾A | 也确实看到一些重复性任务正在被工具接管,**所以这里既不能夸大,也不能假装没有变化** |
| 36 | 嘉宾B | **这里我说的不是某个确定行业马上会发生什么,而是提醒大家,不适应工具的人会承受更高风险** |

**④ 片头剪辑不作为证据。**
播客片头把后面的精彩片段剪到开头做钩子。本篇片头是字幕 1–7,字幕 8 主持人提问才是正片开始。**证据一律指向正片对应位置。**

> 字幕 6 那句"未来三年大量白领岗位会被 AI 替代"**就在片头里** —— 最抓眼球的话本来就是为效果剪进预告的,而说话人在同一句里声明了"我还没有看到明确证据"。

**⑤ 不可证伪的话不抽。** "AI 是把双刃剑""关键在于人"这类句子不能作为要点。

### 保留:两层覆盖

抽取是观点级压缩,97 条字幕只会留下十几个判断 —— 剩下的案例细节、数字、往复展开如果就此消失,编辑写稿时只能回逐字稿从头翻。所以 `material_map.md` 做第二层:**按 outline 的章节把原文全部重排一遍**,已用的和没用的都在,并显式列出判为可弃的部分(广告、寒暄、口误),不静默丢弃。

**关键约束:片段正文由代码从 SRT 取,不由模型复述。** 模型只输出标签和字幕号,代码按号回填原文 —— 让模型抄一遍只会引入错字,且破坏溯源。

---

## 四、如何使用历史素材,以及如何避免误用

> **答案**:四道拦截 —— **同源**由代码算,**不相关 / 过时 / 不适用**由模型判,但相关性的门槛是代码硬校验的。输出只报能用的。

编辑拿这份文件只想知道三件事:**哪一篇 / 用哪一段 / 放在新文章的哪一节。** 不相关的笔记不列 —— 编辑不会因为知道 note_05 讲的是组织管理而做出任何不同的决定。仅当一条都没匹配上时,才需要写清"查了什么、为什么都不适用"。

| 情况 | 输出内容 | 本次实例 |
|---|---|---|
| **同源** | 文件顶部一行报掉,不作为一条建议 | note_02 |
| 匹配到但**受限** | 有限复用 + **原文引用**该篇的可引用范围限制语 | note_01:「仅可作为历史背景或观点参照;不建议作为当前 AI 工具落地、模型能力或市场判断的事实依据」 |
| 匹配到且**可用** | 哪一段、放哪一节、怎么用、**禁止怎么用** | note_06 的默会知识内容 |
| 一条都没匹配到 | 查了什么、按什么标准、为什么都不适用 | 本次不适用 |

### 同源:代码算,不问模型

`historical_notes.jsonl` 第 2 条是本次访谈的**另一版整理稿**,97 行逐条对应。

判据是**字符 8-gram 的有向覆盖率**,`pipeline/textutil.py:ngram_coverage`:

```
覆盖率 = |这篇笔记的 8-gram ∩ 逐字稿的 8-gram| / |这篇笔记自己的 8-gram 总数|
```

分母是**这篇笔记自己**,不是两者并集 —— 它问的是"这篇笔记有多少内容能在逐字稿里找到",是个有向的量。

**两种算法在本数据上的实测对比**(8 篇全跑):

| | 有向覆盖率 | 8-gram Jaccard |
|---|---|---|
| **note_02**(12,709 字) | **0.9122** | **0.8160** |
| 其余 7 篇 | 0.0000 ～ 0.0001 | 0.0000 |

> **诚实说明:在这份数据上 Jaccard 也分得开**,把阈值定在 0.30 两种算法都能正确拦住 note_02。所以"必须用覆盖率"在本次样本上**没有依据**。
>
> 选覆盖率的理由是一个**本次样本观察不到的失效模式**:同源不一定以等长整理稿的形式出现。若某篇历史笔记是同一场访谈的**短摘要**(比如 2,000 字全部摘自 13,000 字的逐字稿),覆盖率仍是 ~1.0,而 Jaccard 会被长度差压到 0.15 左右 —— 低于任何合理阈值,直接漏过。本次 note_02 恰好与逐字稿等长(12,709 vs 12,677 字),Jaccard 才没被稀释。
>
> **阈值该按最坏情况定,不能按唯一一个样本定。** 这是个基于单样本的判断,记在这里供业务方推翻。

**另一个容易混的函数**:`pipeline/srt.py:jaccard` 确实是 8-gram Jaccard,但它干的是另一件事 —— 在 97 条字幕里两两比对找**内容近似重复**(阈值 0.10,实测只捞出 (4, 11) 一对,正是片头预告与正片的重复段)。两侧长度相当,Jaccard 在那里是合适的。

**同源的真实风险不是抄袭,是循环佐证** —— 用素材本身佐证素材本身,并制造"已有历史观点支持"的错觉。此外两版脱敏口径不一致,存在未脱敏信息回流污染当前材料的风险。

**运行时不删库**,只在发 prompt 前过滤掉 —— 换一场访谈它就恢复可用。

### 其余三道:模型判,判据来自篇级元数据

入库时每篇笔记会存一条元数据(`store/notes_meta.jsonl`),字段是题目材料自带的,不是我们造的:

```jsonc
{"note_id":"note_01", "标题":"…", "日期":"2023-07-09", "主题":"…",
 "适用读者":"…", "可引用范围":"…", "正文字数": 41862}
```

这四个字段**原样喂给模型**,并要求它在给出结论时**引用原文**,不做机器解释:

| 拦截 | 谁判 | 判据 | 实例 |
|---|---|---|---|
| **不相关** | 模型提议,**代码否决** | 模型必须填一个 `matched_point`,该值必须是 evidence 主表里真实存在的 `point_id`。填不出或填错 → 整篇不进输出 | 实测:不设限时模型为 7 篇里的 6 篇都找了理由;加了这条硬校验后降到 5 篇 |
| **过时** | **模型** | `日期` + `主题` 的时效性,由模型判断"这个结论今天还成不成立" | note_01 日期 2023-07-09,判为只能作历史参照,不能作事实依据 |
| **不适用** | **模型** | `可引用范围` / `适用读者` 与本次 `content_brief.md` 的目标读者比对 | note_02 的范围写明"如用于企业管理者内容,需说明适用对象差异",而本次目标读者正是管理者 |

**过时和不适用为什么不做成代码规则**:两者都需要读懂自然语言。「可引用范围」写的是整句限制条件(如「不建议作为当前 AI 工具落地、模型能力或市场判断的事实依据」),把它编码成标签需要先定义标签体系,而现在没有依据支撑。时效同理 —— 一篇 2023 年讲组织管理的笔记不算过时,一篇 2023 年讲模型能力的就算,阈值定不出来。

**所以这两道的处理方式是:模型给判断 + 必须附上被引用的原文限制语,标"需编辑确认"。** 工具不替编辑判断"这算不算符合",只保证他能一眼看到限制原文,不用回去翻笔记。

**为什么相关性要做成硬校验**:这是全流程最容易放水的一步。【实测】不设限时模型会为 7 篇里的 6 篇都找出一个理由;要求匹配必须落在主表要点上之后降到 5 篇。主表已经是"这场访谈里真正值得写的东西",一段历史内容如果对不上任何一条要点,编辑不会为了用它专门加一节。

**可引用范围原文引用,不做机器解释。** 该字段是自然语言,编码成标签需要先定义标签体系,而现在没有依据支撑。工具的动作是把限制语**原样贴出来**并标"需编辑确认",不替编辑判断"这算不算符合"。

---

## 五、质量 Rubric

> **答案**:R1–R4 就是题目点名的四个节点(观点提炼 / 文章结构 / 事实证据 / 内容初稿),编号是本文自拟的。每个节点一张表,每条判据标明**谁来验**。
>
> **实际怎么跑**:`python check.py` —— 12 项,读现成的 `outputs/`,逐项打 ✅/❌,退出码即结论。不需要装依赖,不需要 API key。下表把 12 项和 R1–R4 对上。

### 评测实际是怎么执行的

```
$ python check.py
✅ 1. 7 个交付文件存在且非空                    7/7
✅ 2. 所有引用在逐字稿中字面命中                 41/41
✅ 3. 字幕号 ∈ [1,97] 且时间戳落在区间内         全部合法
✅ 4. 无证据指向片头（字幕 1–7）                 0 例外
...
```

| check.py 第几项 | 判据(代码怎么算的) | 属于 |
|---|---|---|
| 1 | 交付文件存在且非空 | — |
| 2 | 每条 `quote` 用 `locate_quote` 回逐字稿搜,全部命中 | R1 |
| 3 | `cue` ∈ [1,97] 且 `ts` 落在该条字幕的时间区间内(容差 1s) | R1 |
| 4 | 没有任何证据的 `cue` 落在片头区间(字幕 1–7) | R1 |
| 5 | 角度数量 == 3 | R2 |
| 6 | 复用建议 ≥1 条,或有非空的 `no_match_reason` | — |
| 7 | 初稿字数 ≥ 300(地板,只判"是不是根本没写完") | R4 |
| 8 | 初稿里每个 `[字幕NN mm:ss 说话人]` 标记,指向的字幕确实存在且说话人对得上 | R4 |
| 9 | note_02 的覆盖率 ≥ 0.30 且它**不在**给编辑的建议列表里 | — |
| 10 | 四个已知待核项("100美元""60%"等)确实被抽取并进了待核清单 | R3 |
| 11 | 字幕 85 的说话人存疑进了风险项 | R3 |
| 12 | 空支撑显式渲染成「无」,且事实类另打红灯 | R1 / R3 |

**这 12 项验的是"这一版合不合规",不验"这一版好不好"** —— 少抽几条要点照样全绿。第 10、11 项是唯一两个内容性断言,靠的是我人工核过逐字稿之后写死的期望值,不是通用判据。

另有 `python scripts/selftest.py` 27 项,验的是更底层的确定性代码(SRT 解析、`locate_quote` 的匹配与自动修正、字数口径、覆盖率函数),不调模型。它保证的是:如果 `locate_quote` 本身有 bug,上面第 2 项的"全部命中"就是假的。

### 四张表:每条判据谁来验

### R1 观点提炼(`evidence.md`)

| 判据 | 通过条件 | 谁验 |
|---|---|---|
| 溯源可验 | 每条原话能在 SRT 里字面命中 | **代码** |
| 位置合法 | 字幕号 ∈ [1,97],时间戳落在该条区间内 | **代码** |
| 不引片头 | 证据不指向字幕 1–7 | **代码** |
| 标签合法 | 类型 ∈ {观点,案例,因果链,事实} | **代码** |
| 空栏显式 | 支撑为空时必须显式渲染"无" | **代码** |
| 限定语保全 | 有限定语的条目被引用时必须带上限定语 | **代码** |
| 是判断不是话题 | 反例:"AI 对教育的影响";正例:"最大的障碍不是技术,是害怕变革的机构" | 人工 |
| 有取舍 | 要点数明显少于话题数。近似 1:1 等于没做取舍 | 人工 |
| 不重复 | 两条要点不表达同一个判断 | 人工 |

**红旗(出现即判不合格)**:不可证伪的句子作为要点。

### R2 文章结构(`outline.md`)

| 判据 | 通过条件 | 谁验 |
|---|---|---|
| 有证据支撑 | 每节至少挂 1 条要点 | **代码** |
| 无静默丢弃 | 未被任何章节采用的要点,在末尾交代 | **代码** |
| 标题是判断句 | 每个章节标题含明确主张,非名词短语 | 人工 |
| 有一条线 | 各章节共同服务于一个主张,不是话题拼盘 | 人工 |
| 顺序有理由 | 体现递进或对照,不是靠"首先/其次/最后"充数 | 人工 |

### R3 事实与证据(`evidence.md` + `editorial_checks.md`)

| 判据 | 通过条件 | 谁验 |
|---|---|---|
| 核实状态显式 | 每项标注:无出处 / 需核实 / 已在原文给出处 | **代码** |
| 风险项落地 | 说话人存疑、口播特征、同源拦截、脱敏口径不一致,均有条目且附原话 | **代码** |
| 待核项全覆盖 | 含数字、比例、年份、机构名的说法 100% 进待核清单 | 规则捞 + 模型判 |
| 断言不升格 | 无出处的条目不得在初稿中以肯定语气出现在结论位 | 规则捞 + 人工 |
| 不越界断言 | 口播段落只表述为"具有推广口播特征",不断言商业关系 | 人工 |

【实测】本次材料的四个待核项,全部无出处:

| 字幕号 | 说法 |
|---|---|
| 38 | "终生收入下降超五分之一""第一年死亡率翻倍"(且被字幕 36 的限定语约束) |
| 43 | "一年 12000 美元小学 / 80000 美元本科 / 现在约 100 美元" |
| 44 | "用 AI 学习能少花 60% 时间" |
| 45 | "不适应的学校 10 年后将不复存在" |

### R4 内容初稿(`draft_v0_5.md`)

| 判据 | 通过条件 | 谁验 |
|---|---|---|
| 证据内联 | 每个核心判断带 `[字幕NN mm:ss 说话人]` 标记 | **代码** |
| 标记有效 | 标记指向的字幕确实由该说话人在该时间说了该内容 | **代码** |
| 字数 | 不判定;仅设 300 字地板判"是不是根本没写完" | **代码** |
| 标题合规 | 无出处/被限定的说法不出现在标题位与结论位 | 规则捞 + 人工 |
| 无正确废话 | 逐段自测:这句话的**反面**能否成立?不能成立的即废话 | 人工 |
| 不是终稿腔 | 不写升华式结语;保留可续写的接口 | 人工 |
| **方向正确** | 编辑的编辑动作以"加"为主。删除率 > 增加率即为失败信号 | **事后测量** |

> **R4 最后一条是整套 Rubric 里最重要的一条,也是唯一一条能真正验证价值的指标。** 前面所有判据都可以在一篇写满的长稿上拿高分,只有它能识别出"AI 替编辑做了不该做的事"。它需要在真实编辑流程里埋点,不是这一版能自测的 —— 这是本方案要向业务方要的第一个数据。

### 初稿的形态:一篇能读的文章,不是提纲的展开

【实测】已发布样稿实际 5,727 / 10,026 / 8,281 字,题目要求 v0.5 是 500–800 字,差一个量级。**字数已向出题方确认「合理即可」**,因此不作门禁。放开长度之后,约束落在形态上:

| ❌ 提纲的展开 | ✅ 初稿 |
|---|---|
| 每节一段,段与段之间没有关系 | 上一段的结尾把下一段的问题带出来 |
| 每段都从"嘉宾X说"开头 | 先立判断,原话用来钉住它 |
| 开头就是第一个小标题 | 有一个把读者拉进来的开场 |
| 写完最后一个要点就结束 | 落在一个判断上,留给编辑续写的接口 |

放开长度**不等于可以写满**。AI 给 8000 字大多是模型自填的过渡句,编辑要**删**;AI 给一篇结构完整的稿,编辑往上**加**。**删比加累。**

---

## 六、异常处理:数据不完整 / 转录有误 / 表达含混 / 素材冲突

> **答案**:**能消歧就消歧,消不了就标记,绝不硬猜。** 具体分三类:**代码扫出来的**(时间戳重叠、超短片段、内容近似重复)、**模型认出来的**(说话人存疑、术语不统一、推广口播、片头)、**代码兜底剔除的**(溯源失败的引用)。

### 三类各自怎么做的

**① 代码扫,`pipeline/srt.py:detect_issues`** —— 解析阶段就跑完,不花 token:

| 异常 | 判据(代码逐条算) | 本次捞到 | 去哪 |
|---|---|---|---|
| 内容近似重复 | 97 条两两比对,字符 8-gram **Jaccard** ≥ 0.10 | (4, 11) 一对,正是片头预告与正片的重复段 | 控制台 |
| 时间戳重叠 | 后一条的 `start` < 前一条的 `end` | 字幕 12/13、80/81、82/83 | 控制台 |
| 超短片段 | `duration` ≤ 1.0 秒 | 12、80、82(0.5s);24、84、86(1.0s) | 控制台 |

> ⚠️ **这三项都只打印到控制台,一条都不进交付物。**
>
> **后两项对编辑没有价值,这是有意的判断,不是遗漏。** 编辑知道字幕 12 和 13 的时间戳差了 0.3 秒,不会因此改一个字。它们唯一的作用发生在数据勘察阶段:看到时间戳不可靠,才定下"**证据定位以字幕号为准、时间戳为辅**"这条规则。规则一旦写进抽取 prompt,运行时再检测就没有下游消费者了。
>
> **所以它们是数据勘察的残留,该删。** 保留在这里只是为了说明那条定位规则的由来。第一项(近似重复)同理:排除片头靠的是抽取 prompt,这个阈值不参与任何排除动作。

**② 模型认,写在 `prompts/extract.md` 里** —— 这几类没有可计算判据,只能靠读懂内容:

| 异常 | prompt 里给模型的判据 | 输出形态 |
|---|---|---|
| 说话人归属存疑 | 上下句是否接得上、后文有没有自相矛盾的应答 | 打 `说话人存疑` 标记 + 写出推理 |
| 术语不统一 | 同一事物在全文里有多种写法 | **列出并列写法,不自行统一** |
| 推广口播 | 主持人单向连续陈述、不含问句、带行动号召(订阅/链接/免费) | 抄原话,**只能表述为"具有推广口播特征",禁止断言商业关系** |
| 片头剪辑 | 开头几条互不衔接、没有问答流 | 证据一律改指向正片对应位置 |

> 为什么片头不写检测算法:它在结构上一眼可见,模型读完全文自己认得出。**为模型顺手能做的事写集合运算,属于自找麻烦。**

**③ 代码兜底,`pipeline/validate.py:drop_unverifiable`** —— 模型引了原文里不存在的话:

- **观点原话**定位不到 → 整条要点剔除
- **限定语 / 支撑**定位不到 → 只剔那一条引用,要点保留
- **每一条被剔除的都写进 `editorial_checks.md`**,附原话与剔除理由

### 落点判断:编辑会不会因此改一个字

同样是"转录有误",时间戳重叠不进 `editorial_checks.md`,说话人存疑要进。分界线是**编辑看到之后会不会做出不同的动作**:时间戳差 0.3 秒,他不会改一个字;说话人可能标错,他必须回看原文才能引用。

| 类别 | 情况 | 本次实例【实测】 | 处理 | 落点 |
|---|---|---|---|---|
| 转录有误 | 时间戳不可靠(重叠、超短片段) | 字幕 12/13、80/81、82/83 重叠;12、80、82 仅 0.5s | 不改原文;**定位一律以字幕号为准,时间戳只作辅助显示**。具体是哪几条不告诉编辑 —— 他不会因此改一个字 | 抽取规则 |
| 转录有误 | **说话人归属存疑** | 字幕 85 标为嘉宾A,但它续接的是字幕 83(嘉宾B)未说完的句子;且字幕 87 嘉宾A 说"完全同意,你说得非常精彩",若 85 也是 A 则说不通 | **不猜**,标出证据与推理 | `editorial_checks.md` |
| 数据不完整 | 内容重复(片头剪辑) | 字幕 4 与字幕 11 近似重复 | 证据指向正片位置 | 抽取规则 |
| 表达含混 | 术语不统一 | Cloud / Claude;凭感觉编码 / 感觉式编程 | 不自行统一,列出并列写法 | `editorial_checks.md` |
| 表达含混 | 指代缺画面无法消歧 | "我记得你的例子是护士录笔记"—— 原始视频未提供 | 标注为缺失信息 | `editorial_checks.md` |
| 素材可信度 | **推广口播** | 字幕 29–30;字幕 53 前半 | 原话抄进风险项,表述为"具有推广口播特征",**不断言商业关系** | `editorial_checks.md` |
| 素材可信度 | **脱敏口径不一致** | 逐字稿里 Claude / Gemini / TikTok 原样出现,而 ChatGPT → "通用AI对话工具" | 两种口径**并列列出**,不替业务方决定;**不做反向识别** | `editorial_checks.md` |
| 素材可信度 | 无出处的事实性说法 | 字幕 38 / 43 / 44 / 45 | 进待核清单;初稿中不作定论 | `editorial_checks.md` |
| 素材冲突 | 历史笔记与本次观点冲突 | 运行时判定 | 说明冲突点,判为不复用或有限复用 | `reuse_suggestions.md` |
| 素材冲突 | 同源循环佐证 | note_02 | 拦截,不作为佐证 | `reuse_suggestions.md` |
| 素材冲突 | 历史内容过时 | note_01(2023-07-09) | 不作为事实依据 | `reuse_suggestions.md` |

**边界情况**:字幕 53 一条字幕内既有口播、又接回了真实访谈问题。处理方式是让模型把口播那句原文抄进风险项,**不需要切分算法** —— 为模型顺手能做的事写检测算法,属于自找麻烦。

---

## 七、这套设计的边界:哪些有保证,哪些没有

题目问的是"如何判断做得好不好"。诚实的回答是:**有一半能保证,一半不能,而且我知道分界线在哪。** 下面三小节分别是:代码能保证的、只靠 prompt 没保证的、完全没有的。

### 能保证的:代码算出来的六件事

不依赖模型自觉。每一条都能指到具体函数,每一条失败都有确定的处置:

| 保证 | 在哪 | 失败时 |
|---|---|---|
| 每条引用在逐字稿里**字面存在** | `textutil.locate_quote` | 重试;仍失败 → 剔除并记入 `editorial_checks.md` |
| 字幕号 / 时间戳 / 说话人**由代码反查**,模型给的值被覆盖 | `validate.repair_quote_ref` | 反查不到 → 同上 |
| 类型与核实状态**必须在枚举内** | `validate.repair_evidence` | 报错重试 |
| 复用建议的落点**必须是主表里真实存在的要点** | `validate.validate_reuse` | 对不上 → 整篇不进输出 |
| 同源笔记由**字符覆盖率**判定,阈值 0.30 | `textutil.ngram_coverage` | ≥ 阈值 → 发 prompt 前就过滤掉 |
| 素材脉络的片段正文**由代码从 SRT 回填** | `validate.validate_material_map` | 字幕号越界 → 报错 |

**这一层是本工具可信度的全部来源。** 它保证的是「这句话确实是他在这个时间说的」,**不保证「他说的是对的」,也不保证「这条值得写」。**

### 不能保证的:只写在 prompt 里、没有任何校验的四件事

| 没保证的 | 具体是什么 | 最坏后果 |
|---|---|---|
| **挑哪几句** | 97 条里留下十几条,取舍全在模型 | **漏掉一条本该写的要点 —— 这是最大的敞口,目前没有任何机制能发现** |
| **打哪个标签** | `观点` / `案例` / `因果链` / `事实` 分错 | 事实类被标成观点 → 逃过"无出处红灯",错误的数字进了正文 |
| **异常认没认出来** | 说话人存疑、口播、术语不统一 | 漏认 → 编辑照常引用一段口播 |
| **复用建议是否牵强** | 过时 / 不适用的判断 | 相关性有硬校验兜底,但"这篇 2023 年的还能不能用"纯靠模型 |

现有的缓解只有三个,**都不是评测**:

1. **把取舍前移到唯一一次调用**(Stage 2)。错误集中在一处,而不是六个阶段各错一点
2. **给模型的选择空间加硬边界**。相关性必须落在要点上、证据必须字面命中 —— 它没法在有校验的地方放水
3. **把不确定的显式暴露给编辑,而不是替他决定**。限制语原文引用、脱敏口径并列列出、可弃项显式列出、被剔除的引用写进待核清单

### 完全没有的:一套能说"质量提升了多少"的评测

R1–R4 里标「人工」的那些判据 —— 有没有取舍、是不是话题拼盘、有没有正确废话 —— **目前只是一份清单,没有抽查流程,没有标注数据,没有基线。** `check.py` 那 12 项验的是合规,不是质量:少抽几条要点照样全绿。

**这不是疏忽,是条件不允许。** 评「观点提炼得好不好」需要一个由编辑标注的黄金集。现有材料是 1 场访谈、8 篇笔记、3 篇样稿,连一个开发集都凑不出。在这个条件下硬造一套评分,得到的是一个看起来严谨的假数字 —— 那比承认没有更糟。

**要把评测建起来,需要向业务方要三样东西,按优先级排:**

| 优先级 | 要什么 | 怎么用 | 能评出什么 |
|---|---|---|---|
| **1** | 试点期编辑动作埋点:改稿时的**增删字数** | 算 删除字数 / 增加字数 | **这工具有没有用。** 比值 > 1 说明 AI 写多了,编辑的时间被转移到删废话上,总耗时可能不降反升。**前面所有判据都能在一篇写满的长稿上拿高分,只有这一条不能** |
| 2 | 3–5 场访谈,编辑**手工标注**"我认为该写的要点" | 与工具抽出的要点集求交 | **召回**:工具漏了哪些。这正好补上第二层最大的敞口 |
| 3 | 已发布成稿 ↔ 其逐字稿的**配对** | 比对成稿里哪些内容逐字稿里没有 | 编辑实际"加"了什么,反推初稿该留什么接口 |

**第 1 项最重要**,它是唯一能证明这工具值不值得用的指标,也是我要向业务方争取的第一个数据。第 2、3 项各需要编辑投入约半天。

### 已知缺口

1. **"无出处不得进结构"没有硬约束。** 本次运行里四条无出处要点恰好都没进结构,但那是结果不是机制保证。
2. **改写型同源可能漏过。** 覆盖率认的是字面重合,大幅改写的同源稿会低于阈值。
3. **style card 只有 3 篇样稿支撑**,能证明它是模板,不足以证明它是固定规范。
4. **段表与素材脉络尚未统一**(见 1.1 的设计方向),目前是两套抽取逻辑。
