# 产出 2：方案设计 —— AI 增强内容复盘工具

> 本文只回答题目产出 2 要求说明的六件事。
> 需求澄清见 `分析/产出1_需求澄清.md`;怎么跑见 `README.md`;完整推演过程见 `分析/PRD_完整版.md`。

**一句话**:把一场一小时访谈的时间序逐字稿,重排成文章序的编辑工作包 —— 判断、证据、结构、初稿各就各位,最后一层取舍留给编辑。

---

## 术语

全文只有两种"编号",不要混:

| 词 | 指什么 | 数量 |
|---|---|---|
| **字幕号** | SRT 文件里每一块字幕自带的序号,即题目说的「段落编号」 | 1–97 |
| **历史笔记段** | 历史笔记被切开后的段落,用于复用匹配,与逐字稿无关 | 87 段 / 8 篇 |

⚠️ 一个字幕号里可能含多句话(如 36 号含十余句),所以**引证据必须同时给字幕号和原话**,只给号定位不到。

---

## 一、全流程:每一步的输入、输出与 AI 介入方式

```
【离线,一次性】
  Stage 0a  历史笔记入库 ──→ 历史笔记段表 + 篇级元数据
  Stage 0b  样稿风格归纳 ──→ style_card.md

【单次运行】
  data/interview_transcript.srt
        │
        ├─ Stage 1  解析(纯代码)────→ 字幕表 × 97
        │
        ├─ Stage 2  观点抽取(LLM×1)→ evidence 主表 ★
        │                              │
        │     ┌────────────────────────┤
   Stage 3    Stage 4              Stage 5
   历史复用    角度生成             结构与初稿
   (LLM×1)    (LLM×1)              (LLM×2)
        └──────────┴──────────────────┘
                       │
                 Stage 6  渲染(纯代码)
                       ↓
   outputs/  angles.md  outline.md  evidence.md
             reuse_suggestions.md  draft_v0_5.md  editorial_checks.md
```

| Stage | 输入 | 输出 | AI 介入方式 |
|---|---|---|---|
| 0a 历史笔记入库 | `historical_notes.jsonl`(8 篇) | 历史笔记段表 + 篇级元数据 | **Prompt**,一篇一次调用 |
| 0b 样稿风格归纳 | `published_samples/*.docx`(3 篇) | `style_card.md` | Prompt 辅助,**人工定稿** |
| 1 解析 | SRT | 字幕表(号 / 起止时间 / 说话人 / 正文)× 97 | **无**。SRT 是结构化格式,正则即可 |
| 2 观点抽取 ★ | 字幕表**全文** + `content_brief.md` | evidence 主表 | **Prompt**,单次调用,全文一次进 |
| 3 历史复用 | 历史笔记段表 + 字幕表全文 + 篇级元数据 | 复用建议(含同源判定) | **Prompt**,单次调用 |
| 4 角度生成 | **evidence 主表** + brief + 标题句式 | 3 个标题/切入角度 | **Prompt** |
| 5a 结构 | evidence 主表 + 选定角度 | `outline.md` | **Prompt** |
| 5b 初稿 | 选定角度 + outline + evidence 主表 | `draft_v0_5.md` | **Prompt** |
| 6 渲染 | 各阶段 JSON | 6 个 Markdown | **无** |

**全部是 Prompt,共 5 次在线调用。没有 RAG,没有 Agent,没有工具调用。** 理由见第六节。

### ★ 主表原则:抽取只跑一次

Stage 2 产出一张 evidence 主表,其余五个文件**不重复抽取**,只做筛选、排序与引用。

| 文件 | 与主表的关系 |
|---|---|
| `evidence.md` | 主表本身 |
| `editorial_checks.md` | 筛"事实类且无出处"的行 + 转录风险项 |
| `outline.md` | 挑要点、组结构 |
| `angles.md` | 从主表选落点 |
| `draft_v0_5.md` | 正文内联证据标记,时间戳与说话人由代码补全 |
| `reuse_suggestions.md` | 历史笔记段表 × 主表的匹配结果 |

直接收益:**证据溯源天然成立**,不需要额外维护一套映射。

### 贯穿全流程的原则:能用代码算出来的事实,不问模型

模型只负责一件不可替代的事:**从原文里挑出哪句话值得引**。

| 项 | 谁提供 | 为什么 |
|---|---|---|
| 引哪句话 | 模型 | 需要判断,代码做不了 |
| 这句话在第几号字幕 | **代码**(按原话反查) | 可算,且模型算错无法被发现 |
| 时间戳、说话人 | **代码**(从字幕表取) | 同上 |
| 这句话是否真的存在于原文 | **代码**(`locate_quote`) | 这是"可溯源"承诺的唯一验证点 |

两个结果:

1. 溯源信息正确**不是靠模型自觉,而是结构上不可能错**。
2. 模型唯一还可能犯的错 —— 引了原文里不存在的话 —— 会被 `locate_quote` 当场抓住。**错误面被压缩到一个可测的点上。**

⚠️ 这条原则也划出能力边界:它保证"这句话确实是他说的",**不保证"他说的是对的"**。后者是编辑的工作。

### 三个关键取舍

**逐字稿不分段。** 1.2 万字任何模型一次读得下,分段会切断跨段关系。实例:字幕 36 的限定语("这里我说的不是某个确定行业马上会发生什么")限定的是字幕 38 的数据 —— 划段极可能把两者分开,第二次调用再也看不见它们的关系。

**生成角度时输入 evidence 主表,不输入逐字稿原文。** 把整篇原文丢给模型让它"提炼角度",等于让它自己找重点。它做不到取舍,只能取平均,输出全篇的最大公约数 —— 也就是那句正确的废话("AI 不会取代你,会用 AI 的人会")。**平庸是这个操作本身决定的,不是提示词写得不好。** 主表已经是做完取舍的结果,喂它进去,模型只需在给定落点上选和写。

> 推论:抽取质量直接决定角度质量。主表抽漏的东西,后面再也找不回来。**Stage 2 是全流程的质量瓶颈。**

**结构与初稿拆成两次调用。** 让初稿基于已定稿的结构生成,保证两个文件不打架。一次调用同时产出结构和正文,模型会在写正文时偏离自己刚定的结构。

---

## 二、编辑做什么、AI 做什么

| 环节 | AI | 编辑 | 为什么这样分 |
|---|---|---|---|
| 通读、结构化、找原话、对时间戳 | ✅ 全做 | — | 机械劳动,AI 快且不会累 |
| 抽取候选要点、打类型标签 | ✅ 提候选 | 审 | AI 提全,编辑砍 —— 漏比多贵 |
| 判断哪条要点值得写 | 提排序建议 | ✅ **定** | 观点价值判断,负责人明确要保留给人 |
| 标出无出处的事实性说法 | ✅ 全做 | 审 | AI 擅长穷举,人擅长判断严重性 |
| **核实**这些说法真假 | ❌ 不做 | ✅ **做** | 无联网、无权威源;AI 核实等于编造置信度 |
| 历史素材匹配、查同源 | ✅ 全做 | 审 | 记忆型劳动,正是编辑现在漏案例的地方 |
| 判断历史素材**能不能用** | 提建议 + 引限制原文 | ✅ **定** | 涉及合规与业务判断 |
| 搭结构、写初稿 | ✅ 出 v0.5 | ✅ **改到 v1** | AI 出骨架,人加理解 |
| 定标题 | 出 3 个备选 | ✅ **选/改** | 标题是立场表达 |
| 发布 | ❌ | ✅ | — |

**一句话概括**:**AI 负责"这话在哪、有没有证据",编辑负责"这话值不值得写、能不能这么说"。**

---

## 三、如何识别和保留关键观点、案例、因果链与原始证据

### 主表结构

```jsonc
{
  "point_id": "P01",                          // 内部连接键,不面向编辑
  "title": "AI使用者与不使用者的差距正在扩大",   // 判断句,不是名词短语
  "type": "观点",                             // 观点 | 案例 | 因果链 | 事实
  "claim":     { "quote": "…", "speaker": "嘉宾B", "cue": 11, "ts": "00:02:09" },
  "qualifier": { "quote": "…", … },           // 限定语,可为 null
  "support":   [ { "quote": "…", … } ],       // 他为什么这么认为:理由 / 推导 / 举例
  "verification": "无需核实",                  // 无需核实 | 需核实 | 无出处
  "flags": [],                                // 片头 | 口播特征 | 说话人存疑 | 术语不统一
  "why": "给编辑的取舍理由,不参与任何自动校验"
}
```

### 五条硬规则

**① 观点原话与支撑原话分开记,支撑一行一条。**
时间戳和说话人是跟着**具体那句话**走的。塞进同一个单元格,初稿就没法在具体句子上挂精确标记。

**② 支撑为空必须显式写"无";但空栏对两类要点意味着不同的事。**

| 类型 | `support` 装的是 | 空着意味着 |
|---|---|---|
| `事实`(含数字、研究、机构、比例) | 他拿什么**证据**支持这个数字 | 🔴 红灯:全篇无出处,写进正文前必须核 |
| `观点` / `案例` / `因果链` | 他**为什么这么认为** | 正常:他直接下了判断没展开,不是缺陷 |

对两者一律打"纯断言"标记是范畴错误 —— 它让「同等教育成本降到约 100 美元」(无出处的事实)和「企业家就是能动性的同义词」(定义式判断,本来就不需要外部背书)长得一模一样,**而区分这两件事恰恰是本工具最该做的**。

**③ 类型标签用题目自己的词:观点 / 案例 / 因果链 / 事实。**
不自造分类体系。打标签的唯一目的是**分流到不同输出文件** —— 不在抽取时标,下游每个文件都得重判一次。

**④ 限定语挂成字段,不作为独立条目。**
限定语不是判断,是对别的判断的约束,单独抽出来下游无法使用。硬规则:**带限定语的条目,引用主张时必须同时带上限定语。** 丢掉限定语再引用,就会把话写满 —— 正是题目点名的失败模式。

【实测】与同源笔记逐句比对后确认,三句限定语**只存在于 SRT**、不存在于已整理版本:

| 字幕号 | 说话人 | 内容 |
|---|---|---|
| 6 | 主持人 | 未来三年大量白领岗位会被 AI 替代……**但我还没有看到明确证据** |
| 26 | 嘉宾A | 也确实看到一些重复性任务正在被工具接管,**所以这里既不能夸大,也不能假装没有变化** |
| 36 | 嘉宾B | **这里我说的不是某个确定行业马上会发生什么,而是提醒大家,不适应工具的人会承受更高风险** |

**⑤ 片头剪辑不作为证据。**
播客片头把后面的精彩片段剪到开头做钩子。本篇片头是字幕 1–7,字幕 8 主持人提问才是正片开始。**证据一律指向正片对应位置。** 做法是写进抽取 prompt,不写检测算法 —— 片头在结构上一眼可见,模型一次读完全文自己认得出。

> 顺带一个值得写进成稿的点:字幕 6 那句"未来三年大量白领岗位会被 AI 替代"**就在片头里** —— 最抓眼球的话本来就是为效果剪进预告的,而说话人在同一句里声明了"我还没有看到明确证据"。

---

## 四、历史素材:如何用,以及如何不误用

题目只要求「**至少 1 条**复用建议,或明确说明为什么不应复用」。

编辑拿这份文件只想知道三件事:**哪一篇 / 用哪一段 / 放在新文章的哪一节。** 所以输出**只报能用的**。不相关的笔记不列 —— 编辑不会因为知道 note_05 讲的是组织管理而做出任何不同的决定。仅当一条都没匹配上时,才需要写清"查了什么、为什么都不适用"。

| 情况 | 输出内容 | 本次实例 |
|---|---|---|
| **同源** | 文件顶部一行报掉,不作为一条建议 | note_02 |
| 匹配到但**受限** | 有限复用 + **原文引用**该篇的可引用范围限制语 | note_01:「仅可作为历史背景或观点参照;不建议作为当前 AI 工具落地、模型能力或市场判断的事实依据」 |
| 匹配到且**可用** | 哪一段、放哪一节、怎么用、**禁止怎么用** | note_06 的默会知识内容 |
| 一条都没匹配到 | 查了什么、按什么标准、为什么都不适用 | 本次不适用 |

### 同源识别由代码算,不问模型

`historical_notes.jsonl` 第 2 条是本次访谈的**另一版整理稿**,97 行逐条对应。

「这篇笔记有多少内容能在这份逐字稿里字面找到」是可计算量 —— 用字符 8-gram 的**有向覆盖率**:note_02 = **0.8913**,其余七篇 ≤ 0.0001。差三个数量级,阈值取 0.30 落在鸿沟正中间。

> 不用 Jaccard 的原因:一篇 1 万字整理稿对 1.3 万字逐字稿,即使逐字对应,交并比也会被长度差稀释;而"这篇笔记有多少内容能在逐字稿里找到"本来就是有向的问题。

**同源的真实风险不是"抄袭",是循环佐证** —— 用素材本身佐证素材本身,并制造"已有历史观点支持"的错觉。此外两版脱敏口径不一致,存在未脱敏信息回流污染当前材料的风险。

**去重发生在两个位置,问的是两个不同的问题**:

- **入库门禁**(`ingest.py`):库内互重 —— 同一篇内容的两个整理版本不能同时进库
- **运行时**(`run.py`):与本次输入同源 —— **不删库**(换一场访谈它就恢复可用),只在发 prompt 前过滤掉

### 剩下三道拦截交给模型,但相关性的门槛是硬校验的

| 拦截 | 判据 | 实例 |
|---|---|---|
| **不相关** | 匹配必须落在 evidence 主表的某条要点上,代码校验;对不上即判不相关,整篇不进输出 | — |
| **过时** | 篇级日期 + 主题时效性 | note_01 日期 2023-07-09,只能作历史参照 |
| **不适用** | 篇级可引用范围 / 适用读者与本次 brief 不符 | note_02 的范围写明"如用于企业管理者内容,需说明适用对象差异",而本次目标读者正是管理者 |

**为什么相关性要做成硬校验**:这是全流程最容易放水的一步。【实测】不设限时模型会为 7 篇里的 6 篇都找出一个理由;要求匹配必须落在主表要点上之后降到 5 篇。主表已经是"这场访谈里真正值得写的东西",一段历史内容如果对不上任何一条要点,编辑不会为了用它专门加一节。

**关键设计:可引用范围原文引用,不做机器解释。** 该字段是自然语言,编码成标签需要先定义标签体系,而现在没有依据支撑。工具的动作是把限制语**原样贴出来**并标"需编辑确认",而不是替编辑判断"这算不算符合"。

---

## 五、质量 Rubric

题目点名的四个节点:观点提炼、文章结构、事实/证据、内容初稿。每项标注是否可自动校验 —— **能自动的做成硬门禁,不能自动的写成人工抽查清单**。

### R1 观点提炼(`evidence.md`)

| 判据 | 通过条件 | 自动 |
|---|---|---|
| 溯源可验 | 每条原话能在 SRT 里字面命中 | ✅ |
| 位置合法 | 字幕号 ∈ [1,97],时间戳落在该条区间内 | ✅ |
| 不引片头 | 证据不指向字幕 1–7 | ✅ |
| 标签合法 | 类型 ∈ {观点,案例,因果链,事实} | ✅ |
| 空栏显式 | 支撑为空时必须显式渲染"无" | ✅ |
| 限定语保全 | 有限定语的条目被引用时必须带上限定语 | ✅ |
| **是判断不是话题** | 反例:"AI 对教育的影响";正例:"最大的障碍不是技术,是害怕变革的机构" | 人工 |
| **有取舍** | 要点数明显少于话题数。近似 1:1 等于没做取舍 | 人工 |
| **不重复** | 两条要点不表达同一个判断 | 人工 |

**红旗(出现即判不合格)**:"AI 是把双刃剑""关键在于人"这类不可证伪的句子作为要点。

### R2 文章结构(`outline.md`)

| 判据 | 通过条件 | 自动 |
|---|---|---|
| 标题是判断句 | 每个章节标题含明确主张,非名词短语 | 人工 |
| 有证据支撑 | 每节至少挂 1 条要点 | ✅ |
| 无静默丢弃 | 未被任何章节采用的要点,在末尾交代 | ✅ |
| **有一条线** | 各章节共同服务于一个主张,不是话题拼盘 | 人工 |
| 顺序有理由 | 体现递进或对照,不是靠"首先/其次/最后"充数 | 人工 |

### R3 事实与证据(`evidence.md` + `editorial_checks.md`)

| 判据 | 通过条件 | 自动 |
|---|---|---|
| 待核项全覆盖 | 含数字、比例、年份、机构名、岗位影响的说法,100% 进待核清单 | 半自动 |
| 核实状态显式 | 每项标注:无出处 / 需核实 / 已在原文给出处 | ✅ |
| 断言不升格 | 无出处的条目不得在初稿中以肯定语气出现在结论位 | 半自动 |
| 风险项落地 | 说话人存疑、口播特征、同源拦截、脱敏口径不一致,均有条目且附原话 | ✅ |
| 不越界断言 | 口播段落只表述为"具有推广口播特征",不断言商业关系 | 人工 |

【实测】本次材料的四个待核项,全部无出处:

| 字幕号 | 说法 |
|---|---|
| 38 | 不适应者"终生收入下降超五分之一""第一年死亡率翻倍"(且被字幕 36 的限定语约束) |
| 43 | "一年 12000 美元小学 / 80000 美元本科 / 现在约 100 美元" |
| 44 | "用 AI 学习能少花 60% 时间" |
| 45 | "不适应的学校 10 年后将不复存在" |

### R4 内容初稿(`draft_v0_5.md`)

| 判据 | 通过条件 | 自动 |
|---|---|---|
| 字数 | 不判定;仅设 300 字地板判"是不是根本没写完" | ✅ |
| 证据内联 | 每个核心判断带 `[字幕NN mm:ss 说话人]` 标记 | ✅ |
| 标记有效 | 标记指向的字幕确实由该说话人在该时间说了该内容 | ✅ |
| 标题合规 | 无出处/被限定的说法不出现在标题位与结论位 | 半自动 |
| **无正确废话** | 逐段自测:这句话的**反面**能否成立?不能成立的即废话 | 人工 |
| **不是终稿腔** | 不写升华式结语;保留可续写的接口 | 人工 |
| **方向正确** | 编辑的编辑动作以"加"为主。删除率 > 增加率即为失败信号 | 事后测量 |

> **R4 最后一条是整套 Rubric 里最重要的一条。** 前面所有指标都可以在写满的长稿上拿高分,只有它能识别出"AI 替编辑做了不该做的事"。

### 初稿的形态:一篇能读的文章,不是提纲的展开

【实测】已发布样稿实际 5,727 / 10,026 / 8,281 字,而题目要求 v0.5 是 500–800 字,差一个量级。**字数已向出题方确认「合理即可」**,因此不作门禁。

放开长度之后,真正的约束落在形态上:

| ❌ 提纲的展开 | ✅ 初稿 |
|---|---|
| 每节一段,段与段之间没有关系 | 上一段的结尾把下一段的问题带出来 |
| 每段都从"嘉宾X说"开头 | 先立判断,原话用来钉住它 |
| 开头就是第一个小标题 | 有一个把读者拉进来的开场 |
| 写完最后一个要点就结束 | 落在一个判断上,留给编辑续写的接口 |

放开长度**不等于可以写满**。AI 给 8000 字大多是模型自填的过渡句,编辑要**删**;AI 给一篇结构完整的稿,编辑往上**加**。**删比加累。**

---

## 六、异常处理:数据不完整 / 转录有误 / 表达含混 / 素材冲突

**总原则:能消歧就消歧,消不了就标记,绝不硬猜。**

| 类别 | 情况 | 本次实例【实测】 | 处理 | 落点 |
|---|---|---|---|---|
| 转录有误 | 时间戳重叠 | 字幕 12/13、80/81、82/83 | 不改原文;定位以**字幕号为准**,时间戳为辅 | 抽取规则。不进待核清单 —— 编辑不会因此改一个字 |
| 转录有误 | 超短片段(≤1s) | 字幕 12、80、82(0.5s);24、84、86(1.0s) | 不单独作证据,并入上下文引用 | 抽取规则 |
| 转录有误 | **说话人归属存疑** | 字幕 85 标为嘉宾A,但它续接的是字幕 83(嘉宾B)未说完的句子;且字幕 87 嘉宾A 说"完全同意,你说得非常精彩",若 85 也是 A 则说不通 | **不猜**,标出证据与推理 | `editorial_checks.md` |
| 数据不完整 | 内容重复(片头剪辑) | 字幕 4 与字幕 11 近似重复 | 证据指向正片位置 | 抽取规则 |
| 表达含混 | 术语不统一 | Cloud / Claude;凭感觉编码 / 感觉式编程 | 不自行统一,列出并列写法 | `editorial_checks.md` |
| 表达含混 | 指代缺画面无法消歧 | "我记得你的例子是护士录笔记"—— 原始视频未提供 | 标注为缺失信息 | `editorial_checks.md` |
| 素材可信度 | **推广口播** | 字幕 29–30;字幕 53 前半 | 原话抄进风险项,表述为"具有推广口播特征",**不断言商业关系** | `editorial_checks.md` |
| 素材可信度 | **脱敏口径不一致** | 逐字稿里 Claude / Gemini / TikTok 原样出现,而 ChatGPT → "通用AI对话工具" | 两种口径**并列列出**,不替业务方决定;**不做反向识别** | `editorial_checks.md` |
| 素材可信度 | 无出处的事实性说法 | 字幕 38 / 43 / 44 / 45 | 进待核清单;初稿中不作定论 | `editorial_checks.md` |
| 素材冲突 | 历史笔记与本次观点冲突 | 运行时判定 | 说明冲突点,判为不复用或有限复用 | `reuse_suggestions.md` |
| 素材冲突 | 同源循环佐证 | note_02 | 拦截,不作为佐证 | `reuse_suggestions.md` |
| 素材冲突 | 历史内容过时 | note_01(2023-07-09) | 不作为事实依据 | `reuse_suggestions.md` |

**边界情况**:字幕 53 一条字幕内既有口播、又接回了真实访谈问题。处理方式是让模型把口播那句原文抄进风险项,**不需要切分算法**。

---

## 七、为什么不用 RAG / Agent

| 技术 | 不用的理由 |
|---|---|
| **RAG / embedding / 向量库** | 检索是为了"从大量内容里找出相关的少量"。这里两侧都不大:逐字稿 1.27 万字一次读得下,历史笔记段表约 8 千字也一次读得下。**没有需要被检索的规模。** 上向量库只会引入召回损失和额外依赖 |
| **chunking** | 同上,且分块会切断跨段关系(字幕 36 限定字幕 38 就是活例) |
| **BM25 / 关键词召回** | 【实测】8 篇笔记的元数据里有 6 篇特征词命中为 **0**。关键词方案在这份数据上直接失效 |
| **Agent 框架** | 流程是固定的 6 个 Stage,DAG 已知,没有需要模型自己决定下一步的地方 |
| **工具调用** | 无外部工具需求。事实核实交给编辑,不做联网核查 |

**规模边界**:单张历史笔记段表能撑到百篇量级。8 篇远未触顶。超过之后再分表或上检索层 —— **那是规模问题,不是这一版的问题。** 提前上 RAG 是拿确定的复杂度换不确定的未来收益。

**依赖清单**:一个 HTTP 客户端 + `python-docx`。就这两个。

---

## 八、已知缺口

诚实交代,不写成"设计如此":

1. **"无出处不得进结构"没有硬约束。** 本次运行里,四条无出处的事实类要点恰好都没进结构,方向与 R4 一致 —— 但那是运行结果,不是机制保证。prompt 里没写这条,代码也没校验。
2. **改写型同源可能漏过。** 覆盖率认的是字面重合,一篇被大幅改写的同源稿会低于阈值。属已知残留风险,不靠换成模型来掩盖。
3. **style card 只有 3 篇样稿支撑**,能证明它是模板,不足以证明它是固定规范。
4. **人工判据没有工具支撑。** Rubric 里标"人工"的那些(有没有取舍、是不是话题拼盘、有没有正确废话)目前只是清单,没有抽查流程。
