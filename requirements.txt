# 依赖只有两个。
# 不引入 jieba / BM25 / embedding 模型 / 向量库 / Agent 框架 / RAG 引擎，理由见 PRD §2.3。

httpx>=0.27          # HTTP 客户端，调 LLM 用
python-docx>=1.1     # 读已发布样稿（sample_03 另有 zipfile 兜底，见 pipeline/docx_read.py）
