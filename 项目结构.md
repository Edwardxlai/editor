# 项目结构

AI 增强内容复盘工具 · 代码地图

本文只回答一个问题：**每个文件是干什么的，改哪里会影响哪里。**
运行方式看 [`README.md`](README.md)，设计论证看 [`PRD.md`](PRD.md)。

---

## 一、全景

```
笔记侠FDE加速营题目一/
│
├── 笔记侠FDE面试题_题目一.pdf     题目原件（最高准则）
├── README.md                      怎么跑：环境、启动、输入、输出
├── PRD.md                         方案设计（产出 2）
├── 项目结构.md                    本文
│
├── run.py                         ★ 主流程：6 阶段编排                484 行
├── ingest.py                      ★ 离线入库：历史笔记 → 历史笔记段表 245 行
├── check.py                       ★ 验收清单：12 项，退出码即结论     257 行
│
├── pipeline/                      内部实现模块（不直接执行）
│   ├── config.py                  路径 / .env / 所有阈值集中声明      225 行
│   ├── llm.py                     厂商无关客户端 + JSON 提取 + 重试   226 行
│   ├── prompts.py                 模板加载（system/user 切分）         41 行
│   ├── srt.py                     SRT 解析 + 转录异常检测             217 行
│   ├── docx_read.py               .docx 读取（含 zipfile 兜底）        73 行
│   ├── textutil.py                原话定位 / 切句 / 字数 / n-gram     206 行
│   ├── validate.py                校验与自动修正（代码算事实）        378 行
│   └── render.py                  JSON → 6 份 Markdown                520 行
│
├── prompts/                       6 个 prompt 模板，与代码分离
│   ├── extract.md   观点抽取（最重，12k 字）
│   ├── reuse.md     历史复用判断
│   ├── angles.md    3 个切入角度
│   ├── outline.md   文章结构
│   ├── draft.md     v0.5 初稿
│   └── segment.md   入库时的历史笔记段抽取（ingest.py 用）
│
├── scripts/                       开发期辅助，验收不需要跑
│   ├── env_check.py               环境预检：零依赖、不 import pipeline 364 行
│   ├── selftest.py                确定性层自检 27 项，不调模型        149 行
│   ├── snapshot.py                改 prompt 前后的输出结构差异对比     166 行
│   └── build_style_card.py        样稿 → 结构画像                      71 行
│
├── data/                          输入（题目方提供，只读）
│   ├── interview_transcript.srt   访谈逐字稿 ← 溯源基准
│   ├── content_brief.md           内容任务背景
│   ├── historical_notes.jsonl     8 篇历史笔记
│   ├── published_samples/*.docx   3 篇已发布样稿
│   └── README_候选人必读.md
│
├── store/                         离线一次性产物（可复用，不随主流程重跑）
│   ├── segments.jsonl             历史笔记段表 ← ingest.py
│   ├── notes_meta.jsonl           笔记篇级元数据 ← ingest.py
│   ├── style_samples_raw.md       样稿结构画像 ← build_style_card.py
│   ├── style_card.md              风格卡（人定稿；结论已写进 prompts/，运行时不读）
│   ├── output_snapshot.json       输出结构基准 ← snapshot.py
│   └── segments.chunked.bak.jsonl 分块版段表备份（已弃用路径的留痕）
│
├── outputs/                       交付物 ← run.py
│   ├── evidence.md                ★ 主表，其余五份都是它的视图
│   ├── angles.md / outline.md / reuse_suggestions.md
│   ├── draft_v0_5.md / editorial_checks.md
│   └── _intermediate/*.json       各阶段中间产物（缓存 / 离线校验 / 评审入口）
│
└── 分析/                          非代码文档
    ├── 产出1_需求澄清.md          题目产出 1
    ├── 产出4_讲解视频脚本.md      题目产出 4
    ├── PRD_完整版.md              PRD 精简前的全本（含未决项论证、数据勘察）
    ├── README_完整版.md           README 精简前的全本
    ├── 改动清单_素材层与去编号.md  待执行的代码改动
    ├── 删减清单_对不回题目的内容.md 按题目 PDF 清点的冗余项
    └── 讨论纪要.md                设计过程记录（追问时的判断依据）
```

---

## 二、三个入口脚本

三个脚本互不调用，各自 `main()` 返回退出码。跑的顺序有依赖，但依赖关系全部落在**文件**上，不在代码里。

| 入口 | 何时跑 | 读 | 写 | 调模型 |
|---|---|---|---|---|
| `scripts/env_check.py` | 换机器后第一条 | 全仓库（只探测） | 无 | 否 |
| `scripts/build_style_card.py` | 一次性 | `data/published_samples/` | `store/style_samples_raw.md` | 否 |
| `ingest.py` | 一次性 | `data/historical_notes.jsonl` | `store/segments.jsonl`、`notes_meta.jsonl` | 是（每篇 1 次） |
| `run.py` | 每次 | `data/*` + `store/*` | `outputs/*` | 是（5 次） |
| `check.py` | 跑完后 | `outputs/*` | 无 | 否 |
| `scripts/selftest.py` | 随时 | 无 | 无 | 否 |

**为什么前两步拆出去：** 样稿和历史笔记是静态的，抽一次永久复用。混进主流程会每跑一次就重读一次 —— 既烧 token，也让风格约束在不同运行间漂移。

---

## 三、run.py 的六个阶段

```
Stage 1  parse_srt           纯代码   逐字稿 → Transcript（cue / 说话人 / 时间戳）
Stage 2  extract    LLM ×1   全文一次进 → evidence.json     ★ 唯一一次读原文做取舍
Stage 3  reuse      LLM ×1   历史笔记段表 × 逐字稿 → reuse.json （发 prompt 前先用代码排同源）
Stage 4  angles     LLM ×1   只喂要点表 → angles.json
Stage 5a outline    LLM ×1   要点表 + 选定角度 → outline.json
Stage 5b draft      LLM ×1   角度 + 结构 + 要点表 → draft.json
Stage 6  render              纯代码   5 份 JSON → 6 份 Markdown
```

每个阶段一个 `Stage` 对象，负责三件事：读缓存（`--cache`）、调模型、落盘 JSON。
`Runner.call()` 是唯一的模型出口 —— `--dry-run` 在这里抛 `DryRun` 中断，所以导出 prompt 这条路径永远不需要 API key。

**关键约束：Stage 2 之后再也不读逐字稿原文。** 下游拿到的是 `evidence_digest()` 生成的要点表文本。取舍在抽取阶段就做完了，下游只做筛选和引用 —— 这也是溯源天然成立的原因：角度引 `P` 编号，`P` 编号本来就挂着说话人和时间戳。

阶段控制参数：

| 参数 | 影响 |
|---|---|
| `--dry-run` | 在第一个未缓存阶段导出 prompt 后停止 |
| `--cache` | 命中 `_intermediate/*.json` 就跳过该阶段 |
| `--until <stage>` | 跑完指定阶段后抛 `StopAfter` |
| `--angle N` | 换第 N 个角度进 outline/draft |

---

## 四、pipeline/ 的分层

依赖方向严格单向，越靠下越无状态：

```
run.py / ingest.py / check.py          入口层
        ↓
render.py      validate.py             产出层 / 校验层
        ↓              ↓
   srt.py   textutil.py   llm.py       原语层
        ↓              ↓        ↓
              config.py                 配置层（谁都可以读，它不读任何人）
```

| 模块 | 职责 | 改动影响面 |
|---|---|---|
| `config.py` | 路径常量、`.env` 解析、`LLMConfig`、**所有阈值** | 全局。改阈值只改这里 |
| `srt.py` | `parse_srt` → `Transcript`（`Cue` 列表、说话人、时长）；三项转录异常检测 | 溯源地基。改了 selftest 立刻红 |
| `textutil.py` | `locate_quote` / `find_offset` / `count_draft_chars` / `ngram_coverage` | 同上。`locate_quote` 是"可溯源"承诺的技术底座 |
| `validate.py` | 每个阶段的校验器 + 自动修正（回填说话人、时间戳、cue 号） | 决定什么算"模型答错了" |
| `llm.py` | HTTP 调用、JSON 提取、按校验器重试、截断识别 | 换厂商/换协议只动这里 |
| `prompts.py` | 读 `prompts/*.md`，按 `@@@USER@@@` 切 system/user，`%%NAME%%` 占位替换 | 极薄，基本不动 |
| `render.py` | 6 个 `render_*` 函数，各写一份 Markdown | 改交付物排版只动这里 |
| `docx_read.py` | 读样稿，python-docx 失败时降级 zipfile 直读 | 只被 `build_style_card.py` 用 |

**分层的实际含义：** `validate.py` 和 `render.py` 之间没有依赖。校验只回答"这份 JSON 能不能用"，渲染只回答"怎么把它写成人能读的文件" —— 所以中间产物 JSON 可以脱离渲染单独校验，`check.py` 就是这么做的。

---

## 五、数据流

```
data/published_samples/*.docx ──build_style_card──▶ store/style_samples_raw.md
                                                          │（人工归纳）
                                                          ▼
                                                    store/style_card.md
                                                          │（结论已抄进）
                                                          ▼
data/historical_notes.jsonl ──ingest──▶ store/segments.jsonl      prompts/draft.md
                                        store/notes_meta.jsonl           │
                                              │                          │
data/interview_transcript.srt ──┐             │                          │
data/content_brief.md ──────────┼──▶ run.py ◀─┘                          │
                                │      │  ◀──────────────────────────────┘
                                │      ▼
                                │  _intermediate/*.json ──render──▶ outputs/*.md
                                │                                       │
                                └───────────────────────────────────────┤
                                                                        ▼
                                                                    check.py
```

三条虚线的含义：
1. `style_card.md` 不在运行时被读取 —— 它的结论已经写进 `prompts/draft.md`。留着是为了留痕：风格约束的来源可查。
2. `run.py` 同时读逐字稿和段表。段表缺失时 reuse 阶段降级并**刻意不写缓存**（否则补跑 ingest 后会被 `--cache` 静默复用，永久丢掉复用判断）。
3. `check.py` 只读 `outputs/`，不重跑任何阶段。

---

## 六、几处需要知道的约定

**阈值只准出现在 `config.py`。**
写死的题目约束（`ANGLE_COUNT=3`、`DRAFT_MIN_CHARS=500`）、MVP 默认值（`MVP_DEFAULTS`）、未定案占位（`TBD_PLACEHOLDERS`）三者分区放置。占位值的使用点会把 `TBD_NOTICE` 写进输出文件，防止被当成业务真理引用。

**prompt 与代码分离。**
prompt 是最需要迭代的部分，改 prompt 不该动代码，也不该埋在字符串里看不见 diff。模板用 `%%NAME%%` 而非 `str.format` —— 模板里有大量 JSON 花括号。

**能用代码算的，不问模型。**
说话人、时间戳、cue 号、同源覆盖率，全部由 `validate.py` / `textutil.py` 从逐字稿反查。模型只负责一件不可替代的事：从原文里挑出哪句话值得引。

**宁可报错，不可伪造。**
任何一步失败都显式抛出并中止，不产出兜底内容。唯一的例外是 extract 阶段：重试用尽后不整批丢弃，改为剔除溯源不了的单条并记账到 `_dropped`，低于下限仍然报错。

**Windows 编码。**
`config.setup_stdout()` 把 stdout/stderr 切 UTF-8 并开 line_buffering —— 控制台默认 GBK 会炸中文，不逐行刷新则单次两三分钟的调用看起来像卡死。所有入口脚本第一件事就是调它。

---

## 七、验证入口

| 命令 | 检什么 | 需要 key | 需要依赖 |
|---|---|---|---|
| `python scripts/env_check.py` | 这台机器现在能跑到哪一步：版本、平台、依赖、文件编码、写权限、`.env` | 否 | 否 |
| `python scripts/selftest.py` | 确定性层 27 项：SRT 解析、原话定位、自动修正、字数口径、同源覆盖率、docx 兜底 | 否 | 否 |
| `python run.py --dry-run` | prompt 内容是否符合预期 | 否 | 否 |
| `python check.py` | 12 项验收：1–8 通用逻辑，9–12 针对本材料包的回归 | 否 | 否 |

**四条命令全部零依赖。** 这不是巧合 ——
`pipeline/llm.py` 把 `import httpx` 延迟到真正发请求时，`pipeline/docx_read.py` 把 `import docx`
延迟并配了 zipfile 兜底，`pipeline/__init__.py` 只声明 `__all__` 不做任何 import。
结果是一台只装了 Python 的机器解压即可完成全部验收，`pip install` 只有在**重新生成**
`outputs/` 时才需要。跨平台验证记录见 README §九。

**先跑 env_check，再跑 selftest。** 前者回答"环境行不行"，后者在零 token 消耗下验证整条链路的地基 —— 这一层必须是确定性的，所以它必须能被离线验证。
